version: "3.8"

services:
  mysql:
    image: mysql:8
    container_name: mysql-university
    environment:
      MYSQL_ROOT_PASSWORD: Cuong@21092004
      MYSQL_DATABASE: student_management
    ports:
      - "3306:3306"
    volumes:
      - mysql_university_data:/var/lib/mysql
    command: --default-authentication-plugin=mysql_native_password

volumes:
  mysql_university_data:
